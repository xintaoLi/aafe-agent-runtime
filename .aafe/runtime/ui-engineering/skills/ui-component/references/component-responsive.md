# Component Responsive

Responsive is strategy, not `flex-col` under 768px.

Tables may become cards or stacked rows. Dialogs may become Drawers on small screens. Selects must remain keyboardable on touch.

Touch targets ≥ 44×44 CSS px on coarse pointers.
