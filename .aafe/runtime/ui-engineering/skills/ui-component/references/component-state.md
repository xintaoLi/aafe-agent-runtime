# Component State

No component enters implementation without a state list.

Shared: default, hover, focus, active, selected, disabled, loading, error, success, empty, readonly, partial.

Button adds loading / destructive. Table adds sorting, filtering, pagination, virtualized.

Missing states are review failures, not polish.
