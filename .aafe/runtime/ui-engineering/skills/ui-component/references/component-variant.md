# Component Variant

Prefer existing variants over new CSS.

Add a variant only when a semantic difference exists (destructive vs default), not when a one-off page wants a slightly different padding.

Variants map to semantic tokens, never to hardcoded palettes.
