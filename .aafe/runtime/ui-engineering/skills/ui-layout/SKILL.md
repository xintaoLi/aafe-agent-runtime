# UI Layout

## Purpose

Turn IA and user flow into page regions.

## Principle

Primary task first. Compose regions from existing layout primitives.

## AntiPattern

Wrapping every section in a Card. Dumping everything into one column under 768px.

## Schema

`.aafe/runtime/ui-engineering/schemas/ui-ux.schema.json`
