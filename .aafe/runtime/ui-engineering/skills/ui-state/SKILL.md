# UI State

## Purpose

No implementation without state design.

## Shared

default, hover, focus, active, selected, disabled, loading, error, success, empty, readonly, partial

## Examples

Button: default, hover, focus, active, disabled, loading, destructive.

Table: loading, empty, error, partial, selected, sorting, filtering, pagination, virtualized.

## Schema

`.aafe/runtime/ui-engineering/schemas/ui-state.schema.json`
